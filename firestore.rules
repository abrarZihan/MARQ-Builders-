rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow full read/write access to all collections for now
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
